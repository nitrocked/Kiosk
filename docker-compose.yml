services:
  sqlserver:
    image: mcr.microsoft.com/azure-sql-edge
    container_name: kiosk-sql-server
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=S3p4l0P@ssw0rd
    ports:
      - "1433:1433"
    volumes:
      - sqlserver_data:/var/opt/mssql

volumes:
  sqlserver_data:
